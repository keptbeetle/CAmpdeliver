{
  "name": "@acme/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./client": {
      "types": "./dist/client.d.ts",
      "default": "./src/client.ts"
    },
    "./schema": {
      "types": "./dist/schema.d.ts",
      "default": "./src/schema.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsc",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint --flag unstable_native_nodejs_ts_config",
    "push": "pnpm with-env drizzle-kit push",
    "security:apply": "pnpm with-env node ./scripts/apply-security-migration.mjs --apply",
    "security:check": "pnpm with-env node ./scripts/apply-security-migration.mjs --check",
    "security:preflight": "pnpm with-env node ./scripts/apply-security-migration.mjs --preflight",
    "payment-destination:apply": "pnpm with-env node ./scripts/apply-payment-destination-migration.mjs --apply",
    "payment-destination:check": "pnpm with-env node ./scripts/apply-payment-destination-migration.mjs --check",
    "payment-destination:preflight": "pnpm with-env node ./scripts/apply-payment-destination-migration.mjs --preflight",
    "payment-hardening:apply": "pnpm with-env node ./scripts/apply-payment-security-hardening.mjs --apply",
    "payment-hardening:check": "pnpm with-env node ./scripts/apply-payment-security-hardening.mjs --check",
    "payment-hardening:preflight": "pnpm with-env node ./scripts/apply-payment-security-hardening.mjs --preflight",
    "email-auth-cleanup:apply": "pnpm with-env node ./scripts/apply-email-auth-cleanup-migration.mjs --apply",
    "email-auth-cleanup:check": "pnpm with-env node ./scripts/apply-email-auth-cleanup-migration.mjs --check",
    "email-auth-cleanup:preflight": "pnpm with-env node ./scripts/apply-email-auth-cleanup-migration.mjs --preflight",
    "studio": "pnpm with-env drizzle-kit studio",
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "drizzle-orm": "^0.44.7",
    "drizzle-zod": "^0.8.3",
    "postgres": "^3.4.9",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@acme/eslint-config": "workspace:*",
    "@acme/prettier-config": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "drizzle-kit": "^0.31.5",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
